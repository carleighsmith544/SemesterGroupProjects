<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SavorySweets"
             x:Class="SavorySweets.App">
        <Application.Resources>
        <!-- 
        <Color x:Key="PageBackgroundLight">#ffffff</Color>
        <Color x:Key="CardBackgroundLight">#fce4ec</Color>
        <Color x:Key="PrimaryTextLight">#d81b60</Color>
        <Color x:Key="SecondaryTextLight">#000000</Color>

    
        <Color x:Key="PageBackgroundDark">#121212</Color>
        <Color x:Key="CardBackgroundDark">#1e1e1e</Color>
        <Color x:Key="PrimaryTextDark">#ff80ab</Color>
        <Color x:Key="SecondaryTextDark">#e0e0e0</Color>

     
        <AppThemeBinding x:Key="PageBackgroundColor"
                         Light="{StaticResource PageBackgroundLight}"
                         Dark="{StaticResource PageBackgroundDark}" />

        <AppThemeBinding x:Key="CardBackgroundColor"
                         Light="{StaticResource CardBackgroundLight}"
                         Dark="{StaticResource CardBackgroundDark}" />

        <AppThemeBinding x:Key="PrimaryTextColor"
                         Light="{StaticResource PrimaryTextLight}"
                         Dark="{StaticResource PrimaryTextDark}" />

        <AppThemeBinding x:Key="SecondaryTextColor"
                         Light="{StaticResource SecondaryTextLight}"
                         Dark="{StaticResource SecondaryTextDark}" />
    -->

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>


    </Application.Resources>
</Application>
